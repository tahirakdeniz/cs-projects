import javafx.application.Application;

/**
 * Main class to start the system. In the command prompt,
 * we can run the system with "java Main" command.
 */
public class Main {
    public static void main(String[] args) {
        Application.launch(App.class, args);
    }
}
